@model AppWebSantaBeatriz.Models.RegisterViewModel
@{
    ViewBag.Title = "Registrar Nuevo Usuario";
}

@*<h2>@ViewBag.Title.</h2>*@
<div class="cont">
    <style>
        .cont {
            background-color: #045eff;
            color: white;
            padding: 10px;
            width:423px
        }

        .textbox {
            border: none;
            border-bottom: solid;
            border-width: 1px;
            background-color: transparent;
            border-color: #7105c6;
            width: 20rem;
            color: white
        }

        .form-group {
            margin-bottom: 10px
        }

        .btningresar {
            margin-top: 10px;
            padding: 8px;
            color: white;
            background-color: transparent;
            border-radius: 25px;
            border: solid;
            border-width: 1px;
            border-color: #7105c6;
            font-weight: 900;
        }
    </style>





    @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
    {
        @Html.AntiForgeryToken()
        <h4>Cree una cuenta nueva.</h4>

        @Html.ValidationSummary("", new { @class = "text-danger" })
        <div class="form-group">
            <label class="form-label">Email</label>
            <div class="col-md-6">
                @Html.TextBoxFor(m => m.Email, new { @class = "textbox" })
            </div>
        </div>
        <div class="form-group">
            <label class="form-label">Nombre de Usuario</label>
            <div class="col-md-6">
                @Html.TextBoxFor(m => m.UserName, new { @class = "textbox" })
            </div>
        </div>
        <!--<div class="form-group">


                <label for="Password">Contraseña</label>
                <div class="col-3">
                    <div class="input-group">
                        <input type="text" name="Password" id="Password" class="form-control" rel="gp" data-size="8" data-character-set="a-z,A-Z,0-9,#">
                        <span class="input-group-btn"><button type="button" class="btn btn-default getNewPass"><span class="fa fa-refresh"></span></button></span>
                    </div>
                </div>-->

        @*@Html.LabelFor(m => m.Password, new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
            </div>*@
        <!--</div>-->
        @*<div class="form-group">
                @Html.LabelFor(m => m.ConfirmPassword, new { @class = "col-md-2 control-label" })
                <div class="col-md-10">
                    @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control" })
                </div>
            </div>*@
<div class="form-group">
    <label class="form-label">Rol de Usuario</label>
    <div class="col-md-6">
        <select name="UserRoles" class="textbox">
            <option style="color:#045eff">-Selecionar-</option>
            @foreach (var row in ViewBag.Name)
            {
                <option style="color:#045eff" value="@row.Value">@row.Value</option>
            }
        </select>
        @*@Html.DropDownList("UserRoles", (SelectList)ViewBag.Name, " ", new {@class = "form-control" })*@
    </div>
</div>
        @Html.HiddenFor(x => x.IDEmpleado)
        @*@Html.HiddenFor(x => x.Nombre)*@
        <div class="form-group">
            <div class="d-flex justify-content-center">
                <input type="submit" class="btningresar" value="Registrar" />
            </div>
        </div>
    }
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
